/**
 * Enum representing supported language codes.
 *
 * - `EN` — English (`'en'`)
 * - `RU` — Russian (`'ru'`)
 * - `UA` — Ukrainian (`'ua'`)
 *
 * This enum is used to define and restrict language options in the system.
 */
export enum ELangs {
  EN = 'en',
  RU = 'ru',
  UA = 'ua',
}
